// 1. Menor de idade.
let idade = 70
let menorIdade = idade < 18 ? "Menor de idade" : "Maior de idade"
console.log(menorIdade)

// 2. Maior de idade.
let maiorIdade = idade >= 18 ? "Maior de idade" : "Menor de idade"
console.log(maiorIdade)

// 3. Idosa (idade >= 65).
let idosa = idade >= 65 ? true : false
console.log(idosa)
